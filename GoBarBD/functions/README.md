## Cloud Functions (FCM)

1) Install Firebase CLI and login.
2) From repo root:
   - `cd functions`
   - `npm install`
   - `firebase deploy --only functions`

This function sends a push notification when a booking status changes.
